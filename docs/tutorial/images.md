# Images and assets

`django-hashtag` renders text chips and does not require images. To match your
site's look and feel, theme the chips with CSS custom properties on the
`hashtag-chip` / `hashtag-chip--<variant>` classes (see
[Templates](templates.md#theming)).
